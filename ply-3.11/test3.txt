Program CheckFunctions; 
VARS int: pe, q;  
float: f1, f2; 

function int TestFunction1(int: x)
VARS int: z; 
{
    z = x + 10; 
    return(x); 
}

function void TestFunction2(int: c, int: param1, int: param2, float: param3)
VARS int: m; 
{
    m = c + 1; 
    write(m); 
}

principal()
{
    VARS int: a, b, c, qu, arreglo[10]; 
    read(a, b, c); 
    write(c, qu); 
    a = 1; 
    b = 2; 
    c = 4; 
    a = b + c; 
    b = pe; 
    if(a < 10) then 
    {
        c = 51; 
    }

    while(b < 100) do
    {
        a = b + c; 
        c = 10;
    }
    write(a+b, c, qu); 
    TestFunction2(a);
}