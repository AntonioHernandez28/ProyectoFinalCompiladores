Program MyRlike; 
VARS 
    int: z, j, p; 
    float: valor; 

function int fact (int: j)
VARS int: i, j, p ;
{
    i = j + p; 
    if(j > 1) then 
    { return (j); }
    else 
    { return (j * fact(j - 1)); }    
} 

function int funint (int : h) 
VARS int: n; 
{
    return(n + h); 
}

function void calcula (int: y)
VARS int: x; 
{
    x = 1; 
    while(x < 11) do 
    {
        x = x * arreglo[x];
        x = x + 1; 
    }
}

principal() 
{
    VARS int: heh, brr, j;  
    read(p); 
    j = p*2; 
    heh = 1;
}