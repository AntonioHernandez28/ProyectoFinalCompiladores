Program MyRlike; 
VARS 
    int: z, j, p, arreglo[1]; 
    float: valor; 

function void fact (int: j)
VARS int: i;
{
    i = j + p; 
    if(j > 1) then 
    { return (j); }
    else 
    { return (j * fact(j - 1)); }    
} 

function void calcula (int: y)
VARS int: x; 
{
    x = 1; 
    while(x < 11) do 
    {
        y = y * arreglo[x];
        x = x + 1; 
    }
    write("acumulado", y); 
}

principal() 
{
    read(p); j = p*2; 
    i = fact(p); 
    for i=1 to 10 do
                {arreglo[i] = p+i;}
    p = Media(arreglo); 
    while(i>0) do 
    {
        calcula(p-i); 
        j = fact(arreglo[i]); 
        write(j, i); 
        i = i + 1; 
    }
}